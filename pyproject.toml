[tool.poetry]
name = "valitar"
version = "0.1.0"
description = ""
authors = ["Enrique Santana <esantanaya@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^5.3"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
